import { FocusListenerDirective } from './focus-listener.directive';

describe('FocusListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusListenerDirective();
    expect(directive).toBeTruthy();
  });
});
